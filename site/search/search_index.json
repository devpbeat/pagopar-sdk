{"config":{"lang":["en","es"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Pagopar SDK Wiki","text":"<p>This wiki documents how to use <code>pagopar-sdk</code> in real applications and how to publish your docs as a Markdown-powered wiki.</p>"},{"location":"#what-you-will-find-here","title":"What you will find here","text":"<ul> <li>A recommended wiki generator setup (MkDocs + Material)</li> <li>End-to-end integration examples for:</li> <li>Django</li> <li>FastAPI</li> <li>Flask</li> </ul>"},{"location":"#install-sdk-and-docs-dependencies","title":"Install SDK and docs dependencies","text":"<pre><code>uv sync --extra docs\n</code></pre> <p>Or with pip:</p> <pre><code>pip install -e \".[docs]\"\n</code></pre>"},{"location":"#run-docs-locally","title":"Run docs locally","text":"<pre><code>mkdocs serve\n</code></pre> <p>Open: <code>http://127.0.0.1:8000</code></p>"},{"location":"django-example/","title":"Django example implementation","text":"<p>This example shows a clean service-oriented integration with <code>pagopar-sdk</code>.</p>"},{"location":"django-example/#1-settings","title":"1) Settings","text":"<pre><code># settings.py\nimport os\n\nPAGOPAR_PUBLIC_KEY = os.environ[\"PAGOPAR_PUBLIC_KEY\"]\nPAGOPAR_PRIVATE_KEY = os.environ[\"PAGOPAR_PRIVATE_KEY\"]\nPAGOPAR_BASE_URL = os.getenv(\"PAGOPAR_BASE_URL\", \"https://api.pagopar.com\")\n</code></pre>"},{"location":"django-example/#2-service-layer","title":"2) Service layer","text":"<pre><code># payments/services/pagopar_service.py\nfrom __future__ import annotations\n\nfrom django.conf import settings\nfrom pagopar_sdk import PagoparBusinessError, PagoparClient\n\n\nclass PagoparService:\n    def __init__(self) -&gt; None:\n        self.client = PagoparClient(\n            public_key=settings.PAGOPAR_PUBLIC_KEY,\n            private_key=settings.PAGOPAR_PRIVATE_KEY,\n            base_url=settings.PAGOPAR_BASE_URL,\n        )\n\n    def create_transaction(self, payload: dict) -&gt; dict:\n        return self.client.commerce.create_transaction(payload)\n\n    def get_order(self, hash_pedido: str) -&gt; dict:\n        return self.client.commerce.get_order(hash_pedido=hash_pedido)\n\n    def close(self) -&gt; None:\n        self.client.close()\n\n\ndef start_checkout(payload: dict) -&gt; dict:\n    service = PagoparService()\n    try:\n        return service.create_transaction(payload)\n    except PagoparBusinessError as exc:\n        return {\"respuesta\": False, \"resultado\": str(exc), \"detalle\": exc.payload}\n    finally:\n        service.close()\n</code></pre>"},{"location":"django-example/#3-api-view-for-checkout","title":"3) API view for checkout","text":"<pre><code># payments/views.py\nfrom __future__ import annotations\n\nimport json\nfrom django.http import JsonResponse\nfrom django.views.decorators.http import require_POST\nfrom .services.pagopar_service import start_checkout\n\n\n@require_POST\ndef create_payment(request):\n    payload = json.loads(request.body)\n    response = start_checkout(payload)\n    return JsonResponse(response, status=200)\n</code></pre>"},{"location":"django-example/#4-url-mapping","title":"4) URL mapping","text":"<pre><code># payments/urls.py\nfrom django.urls import path\nfrom .views import create_payment\n\nurlpatterns = [\n    path(\"payments/create/\", create_payment, name=\"create-payment\"),\n]\n</code></pre>"},{"location":"django-example/#5-webhook-callback-pattern-recommended","title":"5) Webhook callback pattern (recommended)","text":"<p>Use a dedicated endpoint for Pagopar notifications, verify your order in DB, and confirm current state with <code>get_order</code>.</p> <pre><code># payments/views.py\nfrom django.views.decorators.csrf import csrf_exempt\n\n\n@csrf_exempt\n@require_POST\ndef pagopar_webhook(request):\n    event = json.loads(request.body)\n    hash_pedido = event.get(\"hash_pedido\")\n    if not hash_pedido:\n        return JsonResponse({\"ok\": False, \"error\": \"missing hash_pedido\"}, status=400)\n\n    # Reconcile status against Pagopar before marking the order as paid.\n    # status = service.get_order(hash_pedido)\n    return JsonResponse({\"ok\": True})\n</code></pre>"},{"location":"django-example/#good-practices","title":"Good practices","text":"<ul> <li>Keep SDK usage in a service layer, not spread across views.</li> <li>Always reconcile payment state server-to-server (<code>get_order</code>) before fulfillment.</li> <li>Store <code>hash_pedido</code> and your internal order ID for idempotent webhook processing.</li> </ul>"},{"location":"fastapi-example/","title":"FastAPI example implementation","text":"<p>This example uses dependency injection to provide a configured Pagopar client per request.</p>"},{"location":"fastapi-example/#1-configuration","title":"1) Configuration","text":"<pre><code># app/config.py\nfrom pydantic_settings import BaseSettings\n\n\nclass Settings(BaseSettings):\n    pagopar_public_key: str\n    pagopar_private_key: str\n    pagopar_base_url: str = \"https://api.pagopar.com\"\n\n\nsettings = Settings()\n</code></pre>"},{"location":"fastapi-example/#2-dependency-provider","title":"2) Dependency provider","text":"<pre><code># app/deps.py\nfrom collections.abc import Generator\nfrom pagopar_sdk import PagoparClient\nfrom .config import settings\n\n\ndef get_pagopar_client() -&gt; Generator[PagoparClient, None, None]:\n    client = PagoparClient(\n        public_key=settings.pagopar_public_key,\n        private_key=settings.pagopar_private_key,\n        base_url=settings.pagopar_base_url,\n    )\n    try:\n        yield client\n    finally:\n        client.close()\n</code></pre>"},{"location":"fastapi-example/#3-routes","title":"3) Routes","text":"<pre><code># app/main.py\nfrom fastapi import Depends, FastAPI, HTTPException\nfrom pagopar_sdk import PagoparBusinessError, PagoparClient\nfrom .deps import get_pagopar_client\n\napp = FastAPI(title=\"Pagopar Integration API\")\n\n\n@app.get(\"/payments/methods\")\ndef payment_methods(client: PagoparClient = Depends(get_pagopar_client)):\n    return client.commerce.get_payment_methods()\n\n\n@app.post(\"/payments/transaction\")\ndef create_transaction(payload: dict, client: PagoparClient = Depends(get_pagopar_client)):\n    try:\n        return client.commerce.create_transaction(payload)\n    except PagoparBusinessError as exc:\n        raise HTTPException(status_code=400, detail={\"error\": str(exc), \"payload\": exc.payload}) from exc\n\n\n@app.post(\"/payments/recurrent/preauthorize\")\ndef preauthorize(payload: dict, client: PagoparClient = Depends(get_pagopar_client)):\n    return client.recurrent.preauthorize(**payload)\n</code></pre>"},{"location":"fastapi-example/#4-run","title":"4) Run","text":"<pre><code>uvicorn app.main:app --reload\n</code></pre>"},{"location":"fastapi-example/#good-practices","title":"Good practices","text":"<ul> <li>Map <code>PagoparBusinessError</code> to <code>400</code>-style API errors and infrastructure errors to <code>5xx</code>.</li> <li>Keep outgoing payload construction in dedicated service functions for reuse and validation.</li> <li>Add idempotency in your own order handling, especially for retries and webhooks.</li> </ul>"},{"location":"flask-example/","title":"Flask example implementation","text":"<p>This example uses the app factory pattern and a small integration helper.</p>"},{"location":"flask-example/#1-app-factory-configuration","title":"1) App factory configuration","text":"<pre><code># app/__init__.py\nimport os\nfrom flask import Flask\n\n\ndef create_app() -&gt; Flask:\n    app = Flask(__name__)\n    app.config.update(\n        PAGOPAR_PUBLIC_KEY=os.environ[\"PAGOPAR_PUBLIC_KEY\"],\n        PAGOPAR_PRIVATE_KEY=os.environ[\"PAGOPAR_PRIVATE_KEY\"],\n        PAGOPAR_BASE_URL=os.getenv(\"PAGOPAR_BASE_URL\", \"https://api.pagopar.com\"),\n    )\n\n    from .payments import bp as payments_bp\n    app.register_blueprint(payments_bp, url_prefix=\"/payments\")\n    return app\n</code></pre>"},{"location":"flask-example/#2-integration-helper","title":"2) Integration helper","text":"<pre><code># app/pagopar_integration.py\nfrom flask import current_app\nfrom pagopar_sdk import PagoparClient\n\n\ndef build_pagopar_client() -&gt; PagoparClient:\n    return PagoparClient(\n        public_key=current_app.config[\"PAGOPAR_PUBLIC_KEY\"],\n        private_key=current_app.config[\"PAGOPAR_PRIVATE_KEY\"],\n        base_url=current_app.config[\"PAGOPAR_BASE_URL\"],\n    )\n</code></pre>"},{"location":"flask-example/#3-blueprint-routes","title":"3) Blueprint routes","text":"<pre><code># app/payments.py\nfrom flask import Blueprint, jsonify, request\nfrom pagopar_sdk import PagoparBusinessError\nfrom .pagopar_integration import build_pagopar_client\n\nbp = Blueprint(\"payments\", __name__)\n\n\n@bp.get(\"/methods\")\ndef methods():\n    client = build_pagopar_client()\n    try:\n        return jsonify(client.commerce.get_payment_methods())\n    finally:\n        client.close()\n\n\n@bp.post(\"/transaction\")\ndef transaction():\n    payload = request.get_json(force=True)\n    client = build_pagopar_client()\n    try:\n        response = client.commerce.create_transaction(payload)\n        return jsonify(response)\n    except PagoparBusinessError as exc:\n        return jsonify({\"error\": str(exc), \"payload\": exc.payload}), 400\n    finally:\n        client.close()\n</code></pre>"},{"location":"flask-example/#4-run","title":"4) Run","text":"<pre><code>flask --app app:create_app run --debug\n</code></pre>"},{"location":"flask-example/#good-practices","title":"Good practices","text":"<ul> <li>Build and close the SDK client per request unless you manage a global lifecycle explicitly.</li> <li>Keep webhook endpoints separate from checkout creation endpoints.</li> <li>Confirm final payment state with Pagopar before fulfillment.</li> </ul>"},{"location":"wiki-generation/","title":"Wiki generation","text":""},{"location":"wiki-generation/#recommended-tool","title":"Recommended tool","text":"<p>For this SDK, use MkDocs + Material for MkDocs.</p> <p>Why this is a good fit:</p> <ul> <li>Markdown-first workflow (<code>.md</code> files only for content)</li> <li>Fast local preview</li> <li>Easy publish to GitHub Pages</li> <li>Good navigation/search for technical docs</li> </ul>"},{"location":"wiki-generation/#commands","title":"Commands","text":"<p>Install docs dependencies:</p> <pre><code>uv sync --extra docs\n</code></pre> <p>Local preview:</p> <pre><code>mkdocs serve\n</code></pre> <p>Build static site:</p> <pre><code>mkdocs build\n</code></pre>"},{"location":"wiki-generation/#publish-options","title":"Publish options","text":""},{"location":"wiki-generation/#option-a-github-pages-quickest","title":"Option A: GitHub Pages (quickest)","text":"<pre><code>mkdocs gh-deploy --clean\n</code></pre> <p>This creates/updates the <code>gh-pages</code> branch with the generated site.</p>"},{"location":"wiki-generation/#option-b-cicd-pipeline","title":"Option B: CI/CD pipeline","text":"<p>Run <code>mkdocs build</code> in CI and deploy the generated <code>site/</code> folder to your hosting platform.</p>"},{"location":"wiki-generation/#content-structure","title":"Content structure","text":"<p>All documentation content is under <code>docs/</code> and stored as Markdown files:</p> <ul> <li><code>docs/index.md</code></li> <li><code>docs/wiki-generation.md</code></li> <li><code>docs/django-example.md</code></li> <li><code>docs/fastapi-example.md</code></li> <li><code>docs/flask-example.md</code></li> </ul>"},{"location":"es/","title":"Wiki de Pagopar SDK","text":"<p>Esta wiki documenta c\u00f3mo usar <code>pagopar-sdk</code> en aplicaciones reales y c\u00f3mo publicar la documentaci\u00f3n como una wiki basada en Markdown.</p>"},{"location":"es/#que-incluye","title":"Qu\u00e9 incluye","text":"<ul> <li>Una configuraci\u00f3n recomendada para generar la wiki (MkDocs + Material)</li> <li>Ejemplos de integraci\u00f3n completos para:</li> <li>Django</li> <li>FastAPI</li> <li>Flask</li> </ul>"},{"location":"es/#instalar-dependencias-del-sdk-y-de-la-documentacion","title":"Instalar dependencias del SDK y de la documentaci\u00f3n","text":"<pre><code>uv sync --extra docs\n</code></pre> <p>O con pip:</p> <pre><code>pip install -e \".[docs]\"\n</code></pre>"},{"location":"es/#ejecutar-la-documentacion-localmente","title":"Ejecutar la documentaci\u00f3n localmente","text":"<pre><code>mkdocs serve\n</code></pre> <p>Abrir: <code>http://127.0.0.1:8000</code></p>"},{"location":"es/django-example/","title":"Ejemplo de implementaci\u00f3n en Django","text":"<p>Este ejemplo muestra una integraci\u00f3n limpia basada en una capa de servicios con <code>pagopar-sdk</code>.</p>"},{"location":"es/django-example/#1-configuracion","title":"1) Configuraci\u00f3n","text":"<pre><code># settings.py\nimport os\n\nPAGOPAR_PUBLIC_KEY = os.environ[\"PAGOPAR_PUBLIC_KEY\"]\nPAGOPAR_PRIVATE_KEY = os.environ[\"PAGOPAR_PRIVATE_KEY\"]\nPAGOPAR_BASE_URL = os.getenv(\"PAGOPAR_BASE_URL\", \"https://api.pagopar.com\")\n</code></pre>"},{"location":"es/django-example/#2-capa-de-servicio","title":"2) Capa de servicio","text":"<pre><code># payments/services/pagopar_service.py\nfrom __future__ import annotations\n\nfrom django.conf import settings\nfrom pagopar_sdk import PagoparBusinessError, PagoparClient\n\n\nclass PagoparService:\n    def __init__(self) -&gt; None:\n        self.client = PagoparClient(\n            public_key=settings.PAGOPAR_PUBLIC_KEY,\n            private_key=settings.PAGOPAR_PRIVATE_KEY,\n            base_url=settings.PAGOPAR_BASE_URL,\n        )\n\n    def create_transaction(self, payload: dict) -&gt; dict:\n        return self.client.commerce.create_transaction(payload)\n\n    def get_order(self, hash_pedido: str) -&gt; dict:\n        return self.client.commerce.get_order(hash_pedido=hash_pedido)\n\n    def close(self) -&gt; None:\n        self.client.close()\n\n\ndef start_checkout(payload: dict) -&gt; dict:\n    service = PagoparService()\n    try:\n        return service.create_transaction(payload)\n    except PagoparBusinessError as exc:\n        return {\"respuesta\": False, \"resultado\": str(exc), \"detalle\": exc.payload}\n    finally:\n        service.close()\n</code></pre>"},{"location":"es/django-example/#3-vista-para-checkout","title":"3) Vista para checkout","text":"<pre><code># payments/views.py\nfrom __future__ import annotations\n\nimport json\nfrom django.http import JsonResponse\nfrom django.views.decorators.http import require_POST\nfrom .services.pagopar_service import start_checkout\n\n\n@require_POST\ndef create_payment(request):\n    payload = json.loads(request.body)\n    response = start_checkout(payload)\n    return JsonResponse(response, status=200)\n</code></pre>"},{"location":"es/django-example/#4-urls","title":"4) URLs","text":"<pre><code># payments/urls.py\nfrom django.urls import path\nfrom .views import create_payment\n\nurlpatterns = [\n    path(\"payments/create/\", create_payment, name=\"create-payment\"),\n]\n</code></pre>"},{"location":"es/django-example/#5-patron-recomendado-para-webhooks","title":"5) Patr\u00f3n recomendado para webhooks","text":"<p>Usar un endpoint dedicado para las notificaciones de Pagopar, validar el pedido en tu base de datos y reconciliar el estado real con <code>get_order</code>.</p> <pre><code># payments/views.py\nfrom django.views.decorators.csrf import csrf_exempt\n\n\n@csrf_exempt\n@require_POST\ndef pagopar_webhook(request):\n    event = json.loads(request.body)\n    hash_pedido = event.get(\"hash_pedido\")\n    if not hash_pedido:\n        return JsonResponse({\"ok\": False, \"error\": \"missing hash_pedido\"}, status=400)\n\n    return JsonResponse({\"ok\": True})\n</code></pre>"},{"location":"es/django-example/#buenas-practicas","title":"Buenas pr\u00e1cticas","text":"<ul> <li>Mantener el uso del SDK en una capa de servicios.</li> <li>Confirmar el estado del pago desde el servidor antes de entregar el producto.</li> <li>Guardar <code>hash_pedido</code> y tu ID interno para procesar webhooks de forma idempotente.</li> </ul>"},{"location":"es/fastapi-example/","title":"Ejemplo de implementaci\u00f3n en FastAPI","text":"<p>Este ejemplo usa inyecci\u00f3n de dependencias para proveer un cliente configurado de Pagopar por request.</p>"},{"location":"es/fastapi-example/#1-configuracion","title":"1) Configuraci\u00f3n","text":"<pre><code># app/config.py\nfrom pydantic_settings import BaseSettings\n\n\nclass Settings(BaseSettings):\n    pagopar_public_key: str\n    pagopar_private_key: str\n    pagopar_base_url: str = \"https://api.pagopar.com\"\n\n\nsettings = Settings()\n</code></pre>"},{"location":"es/fastapi-example/#2-proveedor-de-dependencias","title":"2) Proveedor de dependencias","text":"<pre><code># app/deps.py\nfrom collections.abc import Generator\nfrom pagopar_sdk import PagoparClient\nfrom .config import settings\n\n\ndef get_pagopar_client() -&gt; Generator[PagoparClient, None, None]:\n    client = PagoparClient(\n        public_key=settings.pagopar_public_key,\n        private_key=settings.pagopar_private_key,\n        base_url=settings.pagopar_base_url,\n    )\n    try:\n        yield client\n    finally:\n        client.close()\n</code></pre>"},{"location":"es/fastapi-example/#3-rutas","title":"3) Rutas","text":"<pre><code># app/main.py\nfrom fastapi import Depends, FastAPI, HTTPException\nfrom pagopar_sdk import PagoparBusinessError, PagoparClient\nfrom .deps import get_pagopar_client\n\napp = FastAPI(title=\"Pagopar Integration API\")\n\n\n@app.get(\"/payments/methods\")\ndef payment_methods(client: PagoparClient = Depends(get_pagopar_client)):\n    return client.commerce.get_payment_methods()\n\n\n@app.post(\"/payments/transaction\")\ndef create_transaction(payload: dict, client: PagoparClient = Depends(get_pagopar_client)):\n    try:\n        return client.commerce.create_transaction(payload)\n    except PagoparBusinessError as exc:\n        raise HTTPException(status_code=400, detail={\"error\": str(exc), \"payload\": exc.payload}) from exc\n\n\n@app.post(\"/payments/recurrent/preauthorize\")\ndef preauthorize(payload: dict, client: PagoparClient = Depends(get_pagopar_client)):\n    return client.recurrent.preauthorize(**payload)\n</code></pre>"},{"location":"es/fastapi-example/#4-ejecutar","title":"4) Ejecutar","text":"<pre><code>uvicorn app.main:app --reload\n</code></pre>"},{"location":"es/fastapi-example/#buenas-practicas","title":"Buenas pr\u00e1cticas","text":"<ul> <li>Mapear <code>PagoparBusinessError</code> a errores <code>400</code> y los fallos de infraestructura a <code>5xx</code>.</li> <li>Construir payloads en funciones o servicios reutilizables.</li> <li>Aplicar idempotencia en pedidos propios y webhooks.</li> </ul>"},{"location":"es/flask-example/","title":"Ejemplo de implementaci\u00f3n en Flask","text":"<p>Este ejemplo usa el patr\u00f3n app factory y un helper peque\u00f1o para la integraci\u00f3n.</p>"},{"location":"es/flask-example/#1-configuracion-en-app-factory","title":"1) Configuraci\u00f3n en app factory","text":"<pre><code># app/__init__.py\nimport os\nfrom flask import Flask\n\n\ndef create_app() -&gt; Flask:\n    app = Flask(__name__)\n    app.config.update(\n        PAGOPAR_PUBLIC_KEY=os.environ[\"PAGOPAR_PUBLIC_KEY\"],\n        PAGOPAR_PRIVATE_KEY=os.environ[\"PAGOPAR_PRIVATE_KEY\"],\n        PAGOPAR_BASE_URL=os.getenv(\"PAGOPAR_BASE_URL\", \"https://api.pagopar.com\"),\n    )\n\n    from .payments import bp as payments_bp\n    app.register_blueprint(payments_bp, url_prefix=\"/payments\")\n    return app\n</code></pre>"},{"location":"es/flask-example/#2-helper-de-integracion","title":"2) Helper de integraci\u00f3n","text":"<pre><code># app/pagopar_integration.py\nfrom flask import current_app\nfrom pagopar_sdk import PagoparClient\n\n\ndef build_pagopar_client() -&gt; PagoparClient:\n    return PagoparClient(\n        public_key=current_app.config[\"PAGOPAR_PUBLIC_KEY\"],\n        private_key=current_app.config[\"PAGOPAR_PRIVATE_KEY\"],\n        base_url=current_app.config[\"PAGOPAR_BASE_URL\"],\n    )\n</code></pre>"},{"location":"es/flask-example/#3-rutas-con-blueprint","title":"3) Rutas con blueprint","text":"<pre><code># app/payments.py\nfrom flask import Blueprint, jsonify, request\nfrom pagopar_sdk import PagoparBusinessError\nfrom .pagopar_integration import build_pagopar_client\n\nbp = Blueprint(\"payments\", __name__)\n\n\n@bp.get(\"/methods\")\ndef methods():\n    client = build_pagopar_client()\n    try:\n        return jsonify(client.commerce.get_payment_methods())\n    finally:\n        client.close()\n\n\n@bp.post(\"/transaction\")\ndef transaction():\n    payload = request.get_json(force=True)\n    client = build_pagopar_client()\n    try:\n        response = client.commerce.create_transaction(payload)\n        return jsonify(response)\n    except PagoparBusinessError as exc:\n        return jsonify({\"error\": str(exc), \"payload\": exc.payload}), 400\n    finally:\n        client.close()\n</code></pre>"},{"location":"es/flask-example/#4-ejecutar","title":"4) Ejecutar","text":"<pre><code>flask --app app:create_app run --debug\n</code></pre>"},{"location":"es/flask-example/#buenas-practicas","title":"Buenas pr\u00e1cticas","text":"<ul> <li>Crear y cerrar el cliente del SDK por request salvo que se gestione un ciclo de vida global.</li> <li>Separar endpoints de checkout y de webhook.</li> <li>Confirmar el estado real del pago con Pagopar antes de entregar el producto.</li> </ul>"},{"location":"es/wiki-generation/","title":"Generaci\u00f3n de wiki","text":""},{"location":"es/wiki-generation/#herramienta-recomendada","title":"Herramienta recomendada","text":"<p>Para este SDK, conviene usar MkDocs + Material for MkDocs.</p> <p>Motivos:</p> <ul> <li>Flujo centrado en Markdown (<code>.md</code> para todo el contenido)</li> <li>Vista previa local r\u00e1pida</li> <li>Publicaci\u00f3n sencilla en GitHub Pages</li> <li>Buena navegaci\u00f3n y b\u00fasqueda para documentaci\u00f3n t\u00e9cnica</li> </ul>"},{"location":"es/wiki-generation/#comandos","title":"Comandos","text":"<p>Instalar dependencias de documentaci\u00f3n:</p> <pre><code>uv sync --extra docs\n</code></pre> <p>Vista previa local:</p> <pre><code>mkdocs serve\n</code></pre> <p>Generar sitio est\u00e1tico:</p> <pre><code>mkdocs build\n</code></pre>"},{"location":"es/wiki-generation/#opciones-de-publicacion","title":"Opciones de publicaci\u00f3n","text":""},{"location":"es/wiki-generation/#opcion-a-github-pages","title":"Opci\u00f3n A: GitHub Pages","text":"<pre><code>mkdocs gh-deploy --clean\n</code></pre>"},{"location":"es/wiki-generation/#opcion-b-cicd","title":"Opci\u00f3n B: CI/CD","text":"<p>Ejecutar <code>mkdocs build</code> en CI y desplegar la carpeta <code>site/</code>.</p>"},{"location":"es/wiki-generation/#estructura-del-contenido","title":"Estructura del contenido","text":"<p>Todo el contenido vive en <code>docs/</code> como archivos Markdown:</p> <ul> <li><code>docs/index.md</code></li> <li><code>docs/wiki-generation.md</code></li> <li><code>docs/django-example.md</code></li> <li><code>docs/fastapi-example.md</code></li> <li><code>docs/flask-example.md</code></li> <li><code>docs/es/index.md</code></li> <li><code>docs/es/wiki-generation.md</code></li> <li><code>docs/es/django-example.md</code></li> <li><code>docs/es/fastapi-example.md</code></li> <li><code>docs/es/flask-example.md</code></li> </ul>"}]}